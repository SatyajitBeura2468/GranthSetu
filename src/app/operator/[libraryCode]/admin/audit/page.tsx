import { Filter } from "lucide-react";
import { Feedback, OperatorPageHeader } from "@/components/operator/page-header";
import { requireLibraryAdministrator } from "@/lib/auth/authorization";
import { getWorkspaceData } from "@/lib/operator/workspace";

type Event = { time: string; actor: string; action: string; entity: string; target: string; result: string };
export default async function AuditPage({ params, searchParams }: { params: Promise<{ libraryCode: string }>; searchParams: Promise<{ error?: string; success?: string }> }) { const [{ libraryCode }, query] = await Promise.all([params, searchParams]); await requireLibraryAdministrator(libraryCode); const data = await getWorkspaceData(libraryCode, "audit"); const events = (data.events ?? []) as Event[]; return <section className="operator-page-v3"><OperatorPageHeader title="Audit" description="Append-oriented activity history, scoped to the current Library Room." /><Feedback error={query.error ?? data.error} success={query.success} /><form className="audit-filters"><Filter aria-hidden="true" /><label>Action<input name="action" placeholder="e.g. circulation" /></label><label>From<input name="from" type="date" /></label><label>To<input name="to" type="date" /></label><button className="button button-secondary button-small">Apply filters</button></form><div className="data-table-wrap"><table className="data-table audit-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Target / context</th><th>Result</th></tr></thead><tbody>{events.map((event) => <tr key={`${event.time}-${event.action}`}><td className="tabular">{event.time}</td><td>{event.actor}</td><td><code>{event.action}</code></td><td>{event.entity}</td><td>{event.target}</td><td><span className="status-dot status-success">{event.result}</span></td></tr>)}</tbody></table></div></section>; }
