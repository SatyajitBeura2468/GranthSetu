import { BookKey, Building2, CalendarRange, IndianRupee } from "lucide-react";
import { workspaceMutationAction } from "../actions";
import { Feedback, OperatorPageHeader } from "@/components/operator/page-header";
import { requireLibraryAdministrator } from "@/lib/auth/authorization";
import { getWorkspaceData } from "@/lib/operator/workspace";

type Setting = { key: string; label: string; value: string };
export default async function SettingsPage({ params, searchParams }: { params: Promise<{ libraryCode: string }>; searchParams: Promise<{ error?: string; success?: string }> }) { const [{ libraryCode }, query] = await Promise.all([params, searchParams]); await requireLibraryAdministrator(libraryCode); const data = await getWorkspaceData(libraryCode, "settings"); const settings = (data.settings ?? []) as Setting[]; return <section className="operator-page-v3"><OperatorPageHeader title="Settings" description="Library identity, academic structure, and policies enforced by the database." /><Feedback error={query.error ?? data.error} success={query.success} /><div className="settings-layout"><nav aria-label="Settings sections"><a href="#identity"><Building2 aria-hidden="true" />Library identity</a><a href="#academic"><CalendarRange aria-hidden="true" />Academic structure</a><a href="#circulation"><BookKey aria-hidden="true" />Circulation policy</a><a href="#fines"><IndianRupee aria-hidden="true" />Fine policy</a></nav><div><section id="identity" className="settings-section"><h2>Library identity</h2><p>The display name is canonical. The public code is globally unique and changing it can break existing links.</p><div className="form-grid"><label>Library display name<input defaultValue="OAV Musiguda Library" disabled={Boolean(data.demo)} /></label><label>Library Code<input className="code-field" defaultValue={libraryCode} readOnly /></label></div></section><section id="circulation" className="settings-section"><h2>Circulation policy</h2><div className="settings-list">{settings.map((setting) => <form action={workspaceMutationAction} key={setting.key}><input type="hidden" name="libraryCode" value={libraryCode} /><input type="hidden" name="operation" value="setting_update" /><input type="hidden" name="settingKey" value={setting.key} /><label><span><strong>{setting.label}</strong><small>Applied only inside this Library Room.</small></span><input name="settingValue" defaultValue={setting.value.replace(/[^0-9]/g, "")} disabled={Boolean(data.demo)} /></label><button className="button button-small button-secondary" disabled={Boolean(data.demo)}>Save</button></form>)}</div></section><section id="academic" className="settings-section"><h2>Academic structure</h2><p>Sessions, classes, and sections preserve historical enrollment context. Configure them before creating student enrollments.</p><button className="button button-secondary" disabled={Boolean(data.demo)}>Manage academic structure</button></section><section id="fines" className="settings-section"><h2>Fine policy</h2><p>Fine amounts remain integer minor units. Enabling fines requires a complete grace-period and daily-rate policy.</p></section></div></div></section>; }
