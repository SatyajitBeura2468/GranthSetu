import { Download, SlidersHorizontal } from "lucide-react";
import { Feedback, OperatorPageHeader } from "@/components/operator/page-header";
import { CategoryBars, CirculationChart } from "@/components/operator/report-chart";
import { getWorkspaceData } from "@/lib/operator/workspace";

type Popular = { title: string; author: string; loans: number };
export default async function ReportsPage({ params, searchParams }: { params: Promise<{ libraryCode: string }>; searchParams: Promise<{ error?: string; success?: string }> }) { const [{ libraryCode }, query] = await Promise.all([params, searchParams]); const data = await getWorkspaceData(libraryCode, "reports"); const summary = (data.summary ?? {}) as Record<string, number>; const popular = (data.popular ?? []) as Popular[]; return <section className="operator-page-v3 reports-page"><OperatorPageHeader title="Reports" description="Operational patterns with exact tables wherever the numbers matter." actions={<a className="button button-secondary" href={`/operator/${libraryCode}/reports/export?kind=circulation`}><Download aria-hidden="true" />Export CSV</a>} /><Feedback error={query.error ?? data.error} success={query.success} /><form className="report-filters"><SlidersHorizontal aria-hidden="true" /><label>From<input type="date" name="from" /></label><label>To<input type="date" name="to" /></label><label>Report<select name="kind"><option>Circulation overview</option><option>Overdue activity</option><option>Inventory distribution</option><option>Fine summary</option></select></label><button className="button button-primary button-small">Apply filters</button></form><div className="report-metrics"><article><span>Issues</span><strong>{summary.issues ?? "—"}</strong></article><article><span>Returns</span><strong>{summary.returns ?? "—"}</strong></article><article><span>Active members</span><strong>{summary.activeMembers ?? "—"}</strong></article><article className={summary.overdue ? "is-urgent" : ""}><span>Overdue</span><strong>{summary.overdue ?? "—"}</strong></article></div><div className="report-grid"><section><header><h2>Circulation over time</h2><p>Completed issue events by period</p></header><CirculationChart values={(data.circulation ?? []) as number[]} /></section><section><header><h2>Catalogue distribution</h2><p>Share of visible titles by category</p></header><CategoryBars values={(data.categories ?? []) as Array<{ label: string; value: number }>} /></section></div><section className="report-table"><header><h2>Most circulated titles</h2><p>Based on recorded loan history, not invented popularity.</p></header><div className="data-table-wrap"><table className="data-table"><thead><tr><th>#</th><th>Title</th><th>Author</th><th>Loans</th></tr></thead><tbody>{popular.map((book, index) => <tr key={book.title}><td>{index + 1}</td><td><strong>{book.title}</strong></td><td>{book.author}</td><td>{book.loans}</td></tr>)}</tbody></table></div></section></section>; }
