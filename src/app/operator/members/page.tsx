import Link from "next/link";
import { requireOperator } from "@/lib/auth/authorization";
import { asOperatorRpcClient } from "@/lib/operator/rpc";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { saveMemberAction } from "./actions";

type MemberRow = { id: string; member_identifier: string; display_name: string; member_kind: string; status: string; active_loans: number; overdue_loans: number; enrollment_label: string | null };
export default async function MembersPage({ searchParams }: { searchParams: Promise<{ q?: string; error?: string; success?: string }> }) { await requireOperator(); const params = await searchParams; const supabase = await createSupabaseServerClient(); const rpc = asOperatorRpcClient(supabase); const { data } = await rpc.rpc("catalogue_members", { p_search: params.q ?? "" }); const members = (data ?? []) as MemberRow[]; return <section className="operator-page" aria-labelledby="members-title"><Link className="auth-secondary-link" href="/operator">← Operator workspace</Link><p className="auth-kicker">Member administration</p><h1 id="members-title">Members and enrollment</h1><p className="operator-lede">Member identity is editable. Active-loan and overdue counts below are derived from circulation history.</p>{params.error ? <p className="auth-error" role="alert">{params.error}</p> : null}{params.success ? <p className="auth-success" role="status">{params.success}</p> : null}<form className="operator-invite" action={saveMemberAction}><h2>Add a member</h2><div className="operator-form-grid"><label className="auth-field">Identifier *<input name="memberIdentifier" required maxLength={120} /></label><label className="auth-field">Display name *<input name="displayName" required maxLength={200} /></label><label className="auth-field">Kind<select name="memberKind" defaultValue="student"><option value="student">Student</option><option value="teacher">Teacher</option><option value="staff">Staff</option><option value="other">Other</option></select></label><label className="auth-field">Status<select name="status" defaultValue="active"><option value="active">Active</option><option value="inactive">Inactive</option><option value="archived">Archived</option></select></label></div><button className="button" type="submit">Create member</button></form><form className="operator-search" method="get"><label className="auth-field">Search members<input name="q" defaultValue={params.q ?? ""} placeholder="Name or identifier" /></label><button className="button button-small" type="submit">Search</button></form><div className="operator-table-wrap"><table className="operator-table"><caption>Members ({members.length})</caption><thead><tr><th>Member</th><th>Kind/status</th><th>Derived borrowing</th><th>Current enrollment</th><th>Controls</th></tr></thead><tbody>{members.map((member) => <tr key={member.id}><th scope="row">{member.display_name}<br /><span className="operator-muted">{member.member_identifier}</span></th><td>{member.member_kind} · {member.status}</td><td>{member.active_loans} active{member.overdue_loans ? ` · ${member.overdue_loans} overdue` : ""}</td><td>{member.enrollment_label ?? "No current enrollment"}</td><td><Link className="button button-small button-quiet" href={`/operator/members/${member.id}`}>Edit</Link></td></tr>)}</tbody></table></div></section>; }
