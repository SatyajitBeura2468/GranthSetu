import Link from "next/link";
import { requireOperator } from "@/lib/auth/authorization";
import { asOperatorRpcClient } from "@/lib/operator/rpc";
import { createSupabaseServerClient } from "@/lib/supabase/server";

const reportNames: Record<string, { label: string; rpc: string }> = { circulation: { label: "Circulation history", rpc: "report_circulation" }, overdue: { label: "Overdue loans", rpc: "report_overdue" }, popular: { label: "Popular books", rpc: "report_popular_books" }, members: { label: "Member activity", rpc: "report_member_activity" }, inventory: { label: "Inventory", rpc: "report_inventory" }, fines: { label: "Fines", rpc: "report_fines" } };
export default async function ReportsPage({ searchParams }: { searchParams: Promise<{ kind?: string }> }) { await requireOperator(); const params = await searchParams; const kind = params.kind && reportNames[params.kind] ? params.kind : "circulation"; const report = reportNames[kind]; const supabase = await createSupabaseServerClient(); const rpc = asOperatorRpcClient(supabase); const { data, error } = await rpc.rpc(report.rpc); const rows = Array.isArray(data) ? data as Array<Record<string, unknown>> : []; const columns = rows.length ? Object.keys(rows[0]) : []; return <section className="operator-page" aria-labelledby="reports-title"><Link className="auth-secondary-link" href="/operator">← Operator workspace</Link><p className="auth-kicker">Operational reporting</p><h1 id="reports-title">{report.label}</h1><p className="operator-lede">Reports read authoritative database state and are bounded for safe operator use.</p>{error ? <p className="auth-error" role="alert">Report could not be loaded.</p> : null}<nav className="operator-actions" aria-label="Report types">{Object.entries(reportNames).map(([key, item]) => <Link key={key} className={`button button-small ${key === kind ? "" : "button-quiet"}`} href={`/operator/reports?kind=${key}`}>{item.label}</Link>)}</nav><p><a href={`/operator/reports/export?kind=${kind}`}>Download CSV</a> · {rows.length} rows</p><div className="operator-table-wrap"><table className="operator-table"><caption>{report.label}</caption><thead><tr>{columns.map((column) => <th key={column} scope="col">{column.replaceAll("_", " ")}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={String(row.id ?? row.loan_id ?? row.book_id ?? row.member_id ?? row.fine_id ?? index)}>{columns.map((column) => <td key={column}>{typeof row[column] === "object" && row[column] !== null ? JSON.stringify(row[column]) : String(row[column] ?? "—")}</td>)}</tr>)}</tbody></table></div></section>; }
